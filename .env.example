DATABASE_URL="postgresql://user:password@localhost:5432/eccd_smarttrack?schema=public"
# Must be at least 32 random characters. Generate with: openssl rand -base64 32
JWT_SECRET="<YOUR_JWT_SECRET>"
JWT_EXPIRES_IN="1d"
PORT=4000
# Required in production — CORS will reject all requests if unset. Never use "*".
CLIENT_ORIGIN="http://localhost:5173"
NODE_ENV="development"
