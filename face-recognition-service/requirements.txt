# Runtime dependencies for the face-recognition microservice.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
python-multipart>=0.0.9
numpy>=1.26,<3
opencv-python-headless>=4.9,<6
Pillow>=10.0

# face_recognition is a well-known wrapper around dlib. Caveat: its last release
# (1.3.0) is from 2020 and it is barely maintained. It is fine for this project,
# but keep all use of it inside recognizer.py so it can be swapped later.
face_recognition==1.3.0
face_recognition_models>=0.3.0
dlib>=19.24   # compiles from source: needs cmake + a C++ toolchain (slow on small machines)
# face_recognition_models imports pkg_resources, which setuptools removed in v81.
setuptools<81
